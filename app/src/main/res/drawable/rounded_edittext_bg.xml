<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="50dp" /> <!-- Ajuste le radius pour définir le degré de rondeur -->
    <solid android:color="@color/colorAccent" /> <!-- Couleur de fond -->
    <stroke
        android:width="1dp"
        android:color="@color/cardview_dark_background" />
</shape>